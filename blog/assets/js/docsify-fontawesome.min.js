(() => {
    "use strict";
    window.$docsify = window.$docsify || {}, window.$docsify.plugins = (window.$docsify.plugins || []).concat((function (c) {
        const i = /:fa[\w -]+:/gm;
        c.beforeEach((c => c.replace(i, (function (c, i) {
            return console.log("m: " + c.replace(/:/gi, "")), `<i class="${c.replace(/:/gi,"")}"></i>`
        })))), c.afterEach((function (c, o) {
            o(c.replace(i, (function (c, i) {
                return console.log("m: " + c.replace(/:/gi, "")), `<i class="${c.replace(/:/gi,"")}"></i>`
            })))
        }))
    }))
})();